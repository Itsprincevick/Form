// import React from "react";
//import "../dist/main.css"; comment this out because thr file does not exist..to avoid errors
import "../src/index.css";
import Form from "./components/form.jsx";

const App = () => {
    return (
        <div>
        <Form />
        </div>
    );
};

export default App;
